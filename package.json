{
  "name": "slidev-loop-monorepo",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "dev": "pnpm -C playground dev",
    "test": "vitest run --passWithNoTests",
    "smoke": "tsx scripts/smoke.ts",
    "sync:instructions": "node scripts/sync-instructions.mjs"
  },
  "devDependencies": {
    "@slidev/cli": "52.16.0",
    "playwright": "^1.54.0",
    "tsx": "^4.20.3",
    "vitest": "^3.0.0"
  }
}
